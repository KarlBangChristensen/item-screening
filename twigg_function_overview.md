# twigg: function overview

Package: **twigg** (version 0.0.0.9000)

functions to help screen IRT data and determine if a Rasch model
is applicable. The implementaion is based on the screening process proposed
by Karl Bang Christensen and Svend Kreiner. The package includes an
artificial example dataset adapted from the eRm package.

- Source: https://github.com/SebastianKvist99/twigg
- Maintainer: Sebastian Kvist <zkb139@alumni.ku.dk>

Generated by `twigg_function_overview.R` on 2026-09-11 07:26.

## Exported functions

The package exports 17 functions and 3 example data sets.

### `build_gllrm_graph`

**Step 5: build the GLLRM graph from screening results**

Constructs the graph implied by the item-screening procedure after genuine local dependence, genuine DIF, and score-covariate associations have been identified. The function returns node and edge tables. Detailed statistical evidence for each edge type is kept in the component-specific edge tables.

```r
build_gllrm_graph( items, covariates, ld = NULL, dif = NULL, step4 =
NULL, score = NULL, score_node = "Score", include_score_node = TRUE )
```

### `build_irt_graph`

**Build the IRT graph implied by a Step 5 graph**

Converts the Step 5 evidence graph into an IRT-style chain graph. The latent variable points to all items, retained score-covariate associations point to the latent variable, DIF edges point from covariates to items, and local dependence edges remain undirected between items.

```r
build_irt_graph(x, theta = "theta", covariate_edges = NULL)
```

### `build_moralized_graph`

**Build the moralized marginal graph implied by a Step 5 graph**

Constructs the Figure 6b-style undirected graph used to read off minimal global Markov property hypotheses. The latent variable is replaced by a total score node, item-score moralization adds item-item edges, and directed parent structures are moralized by adding undirected edges between common parents.

```r
build_moralized_graph(x, score_node = "#", covariate_edges = NULL)
```

### `combine_step3bc`

**Combine Step 3(b) and 3(c) results**

Combine Step 3(b) and 3(c) results

```r
combine_step3bc( step3b_results, step3c_results, original_source_list,
original_dif_list )
```

### `genuine_LD`

**Genuine LD Identifier.**

This function performs step 3a. of the posed item screening. We take the output from the 'screen_LD' function, which flags all LD with a significant partial gamma coefficient and perform a step wise elimination of spurious LD. At each iteration, the item pair with the largest absolute weighted partial gamma coefficient is selected as exhibiting genuine LD. Subsequently, all LD hypotheses conditioned on rest scores defined by either of the selected items, exhibiting the strongest signal for LD, are removed. This procedure is repeated until no significant LD hypotheses remain.

```r
genuine_LD( screen_LD_output, number_of_multiple_tests = NULL, method =
"BH", crit_val = 0.05 )
```

### `M1`

**M1 test for consistency.**

M1 test for consistency.

```r
M1(dataset, items, method = "gamma")
```

### `M2`

**M2 function to test for consistency**

M2 function to test for consistency

```r
M2(dataset, items, method = "gamma")
```

### `M3`

**M3 covariate consistency check**

Performs the M3 screening step of the item screening procedure by assessing monotone associations between external covariates and both the total score and individual item responses. For each covariate, correlations are computed with the total score and with each item separately.

```r
M3(dataset, items, covariates, corr_method = "gamma")
```

### `run_step3b`

**Apply Step 3(b) to all items**

Apply Step 3(b) to all items

```r
run_step3b( dataset, source_list, items, B = 1000, crit_val = 0.05,
p_value_method = c("monte_carlo", "asymptotic", "coin_asymptotic") )
```

### `run_step3c`

**Apply Step 3(c) to all covariates**

Apply Step 3(c) to all covariates

```r
run_step3c( dataset, dif_list, items, B = 1000, crit_val = 0.05,
p_value_method = c("monte_carlo", "asymptotic", "coin_asymptotic") )
```

### `s.d_list`

**Create SOURCE and DIF lists**

We take the output from screen_DIF and create two lists, SOURCE and DIF.

```r
s.d_list(screen_DIF_output, items, covariates)
```

### `screen_DIF`

**Differential item functioning (DIF) identifier**

Performs an initial screening for differential item functioning (DIF) using 'iarm::partgam_DIF'. The function evaluates whether item responses differ systematically across specified covariates.

```r
screen_DIF( dataset, items, covariates, crit_val = 0.05,
number_of_multiple_tests = NULL, method = "BH" )
```

### `screen_items`

**Run item screening procedure**

Runs the item screening procedure by sequentially applying consistency checks M1, M2, and M3 as proposed in the item screening framework. The procedure does NOT stop early if a screening step fails. The final print contains results from all the steps (M1, M2, and M3) and prints explicitly if we passed all steps or not. If we did not pass all steps, the name of the failed steps are printed explicitly. However, we can manually decide whether or not individual steps should be performed, default is that all steps are performed, but can be changed by passing the argument "run_M1 = FALSE" e.g. However M3 is only run if "run_M3=TRUE" (default) and some covariates are passed to the function (otherwise it does not make sense to run it hence it wont).

```r
screen_items( dataset, items, covariates = NULL, method = "gamma",
run_M1 = TRUE, run_M2 = TRUE, run_M3 = TRUE )
```

### `screen_LD`

**Local Dependence (LD) Identifier.**

LD performs an initial screening for local dependecy between items using the 'iarm::partgam_LD' funciton. The function evaluates whether items are indepndent of each other given their rest score. Note that for each pair of items 2 such tests can be performed. Take the item pair (Y_i,Y_j) then we can test both Y_i \bot Y_j \mid S-Y_i and Y_j \bot Y_i \mid S-Y_j. Hence the screening results in 2 dataframes depending on which item is being subtracted from the total score S.

```r
screen_LD(dataset, items, crit_val = 0.05)
```

### `step4_structure_screen`

**Step 4: structural screening of score-covariate associations**

Performs Step 4 of the graphical loglinear Rasch model item-screening procedure. Step 4 is a backwards elimination search for exogenous covariates on which the total score depends. For each covariate, the function tests the conditional association between the total score and that covariate given all other currently retained covariates.

```r
step4_structure_screen( data, items, covariates, score = NULL, alpha =
0.05, adjust_method = NULL, method = "partial_gamma", B = 10000 )
```

### `step6_check_gllrm`

**Step 6: check the GLLRM defined by Step 5**

Performs the first Step 6 check of a graphical loglinear Rasch model by retesting the retained DIF and local-dependence edges from Step 5 under conditioning sets implied by the Step 5 graph. DIF edges are checked using C5-style hypotheses. Local-dependence edges are checked using two C7/C9-style hypotheses based on the LD components containing each item.

```r
step6_check_gllrm( data, step5, items = NULL, covariates = NULL, score
= NULL, alpha = 0.05, adjust_method = NULL, B = 10000 )
```

### `tidy.gllrm_step4`

**Tidy a Step 4 structural screen**

Tidy a Step 4 structural screen

```r
tidy.gllrm_step4(x, ...)
```

## Example data sets

- **`toy_sp_DIF`** -- Toy SPADI pain data with differntial item funcitoning (!). A simulated toy data set for a pain questionnaire to test our functions on. Important note: This data set does contain items which exhibit DIF!
- **`toy_sp_DIF_and_LD`** -- Toy SPADI pain data with both LD and DIF (!). A simulated toy data set for a pain questionnaire to test our functions on.
- **`toy_spadi_pain`** -- Toy SPADI pain data. A simulated toy data set for a pain questionnaire to test our functions on.

